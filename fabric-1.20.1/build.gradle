plugins {
    id "fabric-loom"
}

base {
    archivesName = "${rootProject.property("archives_base_name")}-fabric-${rootProject.property("minecraft_1201")}"
}

dependencies {
    minecraft "com.mojang:minecraft:${rootProject.property("minecraft_1201")}"
    mappings loom.officialMojangMappings()
    modImplementation "net.fabricmc:fabric-loader:${rootProject.property("fabric_loader_version")}"

    modCompileOnly files(rootProject.file("deps/sodium-fabric-1.20.1.jar"))
    modRuntimeOnly files(rootProject.file("deps/sodium-fabric-1.20.1.jar"))
}

loom {
    mixin {
        useLegacyMixinAp = false
    }
}

sourceSets {
    main {
        java.srcDir(rootProject.file("common-src/1.20.1/java"))
        resources.srcDir(rootProject.file("common-src/1.20.1/resources"))
    }
}

processResources {
    def props = [
            version          : project.version,
            minecraft_version: rootProject.property("minecraft_1201"),
            loader_version   : rootProject.property("fabric_loader_version")
    ]

    inputs.properties(props)

    filesMatching("fabric.mod.json") {
        expand(props)
    }
}
